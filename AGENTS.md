# AGENTS.md
- Supabase 규칙: 순수 HTML·CSS·JavaScript 구조는 유지하고 데이터 동기화에 버전이 고정된 Supabase 브라우저 SDK를 사용할 수 있다.
- 데이터 보안: 브라우저에는 Supabase publishable key만 두고 비밀 키를 넣지 않으며, 업무 테이블은 사용자 소유권 기반 RLS를 유지한다.
- 만드는 것: 여러 거래처의 업무를 기록하고 마감시간이 가까운 순서대로 처리하도록 돕는 도구다.
- 기술 규칙: 순수 HTML, CSS, JavaScript만 사용하고 설치·빌드·프레임워크를 추가하지 않는다.
- 먼저 읽을 자료: 작업 전에 `docs/prd.md`, `docs/design.md`, `docs/memory.md`를 읽는다.
- 요구사항 기준: 기능은 `docs/prd.md`, 화면과 색상·여백·문구는 `docs/design.md`를 따른다.
- 변경 원칙: 기존 데이터 저장, 마감 정렬, 상태 표시, 수정·취소·처리, 별도 완료 기록과 후속 업무 추가 기능을 보존한다.
- 정렬 원칙: 미처리 업무는 상태, 마감시간, 같은 거래처의 접수 번호 순으로 비교하며 접수 번호가 실제 마감 우선순위를 앞서지 않게 한다.
- 반복 절차: 수정 후 JavaScript 구문을 검사하고 휴대폰 폭을 포함해 브라우저에서 다시 확인한다.
- 완료 보고: 변경한 내용과 적용 파일, 확인 결과를 사용자에게 알린다.
- 파일 구조: `index.html`과 `AGENTS.md`는 최상위에 두고, 실행 자원은 `assets`, 프로젝트 문서는 `docs`에 분류한다.
- 컴포넌트 규칙: 재사용 가능한 순수 JavaScript UI는 `assets/components`에 CSS와 JavaScript를 함께 분류하고 프레임워크 의존성을 추가하지 않는다.
- 작업 기록: 핵심 기능의 추가·수정·제거 또는 핵심 구조 변경이 있으면 작업 종료 후 한국 시간 기준 일자와 시간을 `docs/memory.md`에 기록한다.
- AI 개발팀: 프로젝트 전용 에이전트는 `.codex/agents`에 있으며 HTML은 `짱구`, CSS는 `철수`, JavaScript는 `훈이`, QA는 `맹구`에게 위임한다.
- 위임 원칙: 사용자가 특정 에이전트를 지목하면 그 에이전트를 사용한다. 단일 영역 변경은 담당 에이전트에게 맡기고, 복합 변경은 충돌을 피하도록 HTML → CSS → JS 순서로 위임한 뒤 `맹구`에게 최종 QA를 맡긴다.
- 병렬 원칙: 읽기·분석·검증은 병렬로 실행할 수 있지만 같은 파일을 수정할 가능성이 있는 작업은 병렬로 맡기지 않는다. 메인 에이전트는 결과를 통합하고 문서와 `docs/memory.md`를 최종 갱신한다.
- 확인 필요: 지원할 최소 브라우저와 구체적인 모바일 기준 폭.
